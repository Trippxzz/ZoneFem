// --- Funciones del Carrito (LEGACY - Ya no se usa) ---
// El carrito ahora se maneja completamente desde el backend de Django
// Este archivo se mantiene solo para compatibilidad con funciones antiguas

function showNotification(message) {
  console.log('Notification:', message);
  // Si quieres mantener notificaciones, podrías implementarlas aquí
}
